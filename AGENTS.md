# AndroidMVIBase 项目约定

## 范围与学习节奏

- 这是用于学习和未来项目复用的独立 Android 基础框架。先核对当前仓库与任务范围；目标不一致时只读排查并确认准确路径，不跨工程修改。
- 采用 Compose + 轻量 MVI 方向、Hilt/KSP、Retrofit/OkHttp、Coroutines/Flow；不要套用其他旧 Android 工程的 XML 或基类约定。
- 每次只推进当前学习步骤；开始前明确“这一步需要你学习”，说明目标、阅读文件、概念和验收方式。结束时给出练习和下一步，等用户反馈再进入下一学习阶段；尊重用户明确指定的连续执行范围。
- 新增模块与 app 同级，不增加 core 层。新模块必须有 README，能力或公共 API 变化时同步更新。

## Skill 路由

- 编写、封装、修复、讲解、维护或 Review 本框架及示例宿主时，读取 [维护者 Skill](skills/android-mvi-base-maintainer/SKILL.md)，按 [框架开发规范](docs/framework-development.md) 执行。
- 在业务工程接入或 Review 框架用法时，读取 [使用者 Skill](skills/android-mvi-base/SKILL.md) 和相关模块 README；不把本仓库教学流程、业务包名或内部模块约定复制到接入方。
- 混合任务分别明确框架修改与业务接入范围。维护者规范与使用者契约各司其职，不互相替代。
- skills/ 由本文件路由；工具未自动发现时直接读取文件，不宣称已自动加载。
- 总览与学习进度见 [README.md](README.md)。不默认加载所有模块的详细文档。

## 核心约束

- 以真实源码为准，明确区分已实现、拟议示例和待完成能力；README 与源码不一致时报告差异。
- 页面不直接持有 Retrofit/OkHttp/API；优先复用已有公共能力。维护者可在授权范围内演进公共 API，同时说明调用方影响并同步文档；不另起同义封装。
- 新页面的精确包路径和 MVI API 尚未落地，应在对应设计步骤中明确，不假定已有 BaseViewModel、Event 或 Effect 类。
- Review 先给源码证据和影响，未经要求不直接修复；风险不能因“赶时间”或“可编译”省略。
- 验证与改动匹配；仅文档修改检查内容和链接，不为其重复构建。编译通过不等于设备或服务端行为通过。
- 不自动提交、推送或修改全局 Skills。提交前展示准确范围和提交说明，获得用户明确确认。
